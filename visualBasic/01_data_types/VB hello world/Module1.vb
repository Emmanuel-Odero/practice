Module Module1

    Sub Main()
        Console.WriteLine("Hello World")

        'Variable declaration
        Dim myNum As Integer
        myNum = 5
        Dim myString As String = "This is just my string"
        Dim myChar As Char = "a"
        Dim myFloat As Double = 5.43
        Dim myBool As Boolean = True

        'equating a variable to nothing results to no erro
        'if the variable is not used
        Dim answer As Interger = Nothing

        Console.WriteLine(myNum.ToString())
        Console.WriteLine(myString)
        Console.WriteLine(myNum)
        Console.ReadLine()


    End Sub

End Module
